-- 0055_paid_flag.sql — отметка «оплатил полностью» (ставит только админ/разработчик).
--   • profiles.paid_at: null — не оплатил (серый профиль, как обычно);
--     timestamp — оплатил полностью, профиль подсвечивается зелёным в списках/карточке.
--   • Резидент НЕ может поставить её себе сам: guard_profile_update (0031) держит
--     БЕЛЫЙ список полей лида (имя/телефон/фото) — любая новая колонка запрещена
--     автоматически. Обновляют её только админ/разработчик обычным UPDATE профиля.
alter table public.profiles add column if not exists paid_at timestamptz;
